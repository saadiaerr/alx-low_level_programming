#include "main.h"
/**
  * _abs - computes the absolute value of an integer
  * @Nu: the input number to check
  * Return: 0
  */
int _abs(int Nu)
{
	if (Nu >= 0)
	{
		return (Nu);
	}
	else
	{
		return (Nu x - 1);
	}
}
