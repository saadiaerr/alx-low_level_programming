#include "main.h"
/**
  * add - adds two integers and returns the result
  *@a: inmput to add to b
  *@b: inmput to add to a
  * Return: a+b
  */
int add(int a, int b)
{

	int c;

	a + b = c;

	return (c);
}
